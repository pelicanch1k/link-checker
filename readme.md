````markdown
# Link Checker

Link Checker — это веб-сервис на Go, который принимает URL, загружает HTML-страницу, извлекает все ссылки и проверяет их доступность. Результат проверки возвращается в формате JSON или PDF.

---

## 🚀 Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone https://github.com/pelicanch1k/link-checker.git
cd link-checker
````

### 2. Скачать зависимости

```bash
go mod download
```

или

```bash
go mod tidy
```

### 3. Запуск сервера

```bash
cd cmd
go run .
```

По умолчанию сервер работает на:

```
http://localhost:8080
```

---

## 📡 API

### POST /check

Проверяет все ссылки на указанной HTML-странице или страницах.

#### Тело запроса

```json
{
    "links": ["google.com"]
}
```

#### Пример запроса

```bash
curl -X POST http://localhost:8080/check \
  -H "Content-Type: application/json" \
  -d '{"links": ["google.com"]}'
```

#### Пример ответа

```json
{
    "links": {
        "google.com": "available"
    },
    "links_num": 1
}
```

## 🧠 Как работает сервис

1. Принимает URL через POST /check
2. Загружает HTML-страницу
3. Извлекает все ссылки (`<a href="...">`)
4. Преобразует относительные URL в абсолютные
5. Параллельно проверяет каждую ссылку
6. Возвращает JSON с валидными и невалидными ссылками

---

### POST /info

Принимает айдишники ранее добавленых ссылок и проверяет их, результат отдает в PDF

#### Тело запроса

```json
{
    "links_list": [1, 2]
}
```

#### Пример запроса

```bash
curl -X POST http://localhost:8080/check \
  -H "Content-Type: application/json" \
  -d '{"links_list": [1, 2]}'
```

#### Пример ответа

```
PDF-файл
```

## 🧠 Как работает сервис

1. Принимает URL через POST /info
2. Достает из БД все ссылки по указаным айди
3. Параллельно проверяет каждую ссылку
4. Возвращает PDF с валидными и невалидными ссылками

---

## 📁 Структура репозитория

```
/cmd
    main.go — Точка входа в программу

/internal/adapter
    Работа с внешними системами(БД, PDF)

/internal/checker
    Содержит основной usecase

/internal/controller/http
    Основной конроллер принимающий запросы

/interlnal/domain
    Содержит основные структуры домена

go.mod
go.sum
```

---

## 🔧 Какие ошибки обрабатываются

* Статусы 4xx / 5xx
* Не существующие домены
* Таймауты
* Ошибки сетевого уровня
* Ошибки загрузки HTML
* Некорректные URL

---
